function setup() {
  colorMode(HSL)
  createCanvas(400, 400);
}

function draw() {
  background(220);
  noStroke()
  
  let w = 400/step
  for (let i = 0; i < step; i++)
let O = color(19,78.2,38.1)
let B = color(212,46.9,68)
  
  
}